let city = ["Chennai", "Mumbai", "Delhi", "Pune", "Kolkata"];

for (let i = 1; i < city.length; i++) {
    if (i % 2 != 0) {
    //if (i % 2 === 0) {
        console.log(city[i]);
    }
 console.log(city.length);
}